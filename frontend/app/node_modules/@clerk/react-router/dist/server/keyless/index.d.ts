import { createKeylessService } from '@clerk/shared/keyless';
import { DataFunctionArgs } from '../loadOptions.js';
import { ClerkMiddlewareOptions } from '../types.js';
import '@clerk/backend/internal';
import 'react-router';
import '@clerk/backend';
import '@clerk/shared/types';

/**
 * Gets or creates the keyless service singleton.
 * Returns null for non-Node.js runtimes (e.g., Cloudflare Workers).
 */
declare function keyless(args: DataFunctionArgs, options?: ClerkMiddlewareOptions): Promise<ReturnType<typeof createKeylessService> | null>;
/**
 * @internal
 */
declare function resetKeylessService(): void;

export { keyless, resetKeylessService };
